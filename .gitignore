# ============================================
# SECRETS — never commit
# ============================================
www/config.php
www/crontoken.txt

# ============================================
# Runtime data (regenerated by cron/proxies)
# ============================================
# API response caches
www/sc_data_cache.json
www/sck_data_cache.json
www/pa_data_cache.json
www/cache/*.json
www/cache/*.csv

# SQLite database (rolling 5-day window, regenerated by collect.php)
www/historygraph/*.db
www/historygraph/*.db-journal
www/historygraph/*.db-wal
www/historygraph/*.db-shm

# Monitor cooldown state
www/historygraph/monitor_last_alert.txt

# Legacy/generated sensor dump
www/cz_sensors.json

# ============================================
# Editor / OS / tooling
# ============================================
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Old backup folders (historical versions tracked in git history instead)
www/backup/
www/historygraph/backup/

# FTP/deploy artifacts
*.bak
*.orig
